export const items = (state) => state.tasks.items;
export const loading = (state) => state.tasks.loading;
export const error = (state) => state.tasks.error;
export const selectedTask = (state) => state.tasks.selectedTask;
